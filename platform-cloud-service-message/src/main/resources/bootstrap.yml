server:
  port: 8004

spring:
  profiles:
    active: dev
  application:
    name: glcxw-cloud-message

logging:
  file:
    max-size: 500MB
    max-history: 7
    path: logs/${spring.application.name}
