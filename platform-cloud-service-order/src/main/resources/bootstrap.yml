server:
  port: 8005

spring:
  profiles:
    active: dev
  application:
    name: glcxw-cloud-order

logging:
  file:
    max-size: 500MB
    max-history: 7
    path: logs/${spring.application.name}
