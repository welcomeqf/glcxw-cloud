server:
  port: 8010

spring:
  profiles:
    active: dev
  application:
    name: glcxw-cloud-springbootadmin

logging:
  file:
    max-size: 500MB
    max-history: 7
    path: logs/${spring.application.name}
